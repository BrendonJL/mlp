---
id: "2026-02-23"
aliases: []
tags:
  - type/daily
  - type/project
created: 2026-02-23
---
# Sunday, February 23, 2026

> _Week 08 of 2026_

## Session Focus

v9 training monitoring and results review. Eval script updates for screen recording workflow.

## Goals

- [x] Review v9 training results (still running, ~9.4M/10M steps)
- [x] Write SQL comparison query for all experiments
- [x] Update `evaluate_all_models.py` for screen recording workflow
- [ ] Run eval with `--render` for demo video recording
- [ ] Run headless eval (50 episodes) for final statistics

## What I Accomplished

### v9 Results (In-Progress Training)

- **89.2% completion rate** (distance-based, x_pos >= 3150) — up from 5.2% (v8) and 0% (all prior)
- Avg distance: 3821 (routinely clearing 1-1, pushing ~670px into 1-2)
- Avg reward: 7912 (highest of any experiment)
- Max distance: 5288 (deepest into 1-2 of any model)
- 13,966 episodes logged, training still running (~600K steps remaining)
- Terminal shows ~905 x_pos (raw stage 2 position) — WandB correctly logs accumulated ~4000

### Eval Script Updates

Added three features to `scripts/evaluate_all_models.py` for screen recording workflow:

1. **`--render` flag** — toggles `render_mode="human"` to open game window for recording
2. **Wall-clock timestamps** — every episode logs `@HH:MM:SS` in output
3. **Best episode summary** — after each model AND at the end, prints best episode with recording offset (`00:05:23-00:06:41` format) for LosslessCut trimming

### DB Query Setup

- Fixed DBUI (vim-dadbod-ui) connection — password had stale `!` suffix from old Fedora
- Removed hardcoded `vim.g.dbs` from Neovim config, now using DBUI's connection manager
- Wrote experiment comparison query using distance-based completion (`COALESCE(max_x_pos, distance_traveled) >= 3150`) instead of unreliable `flag_get`
- Wrote recent-1000-episodes query for v9 current performance

### Key Finding: `level_completed` Column Unreliable

The `level_completed` column uses `flag_get` which is flaky. Distance-based detection (`x_pos >= 3150`) gives accurate results:
- v8: 9.2% (flag_get) → 5.2% (distance) — fewer actual completions than reported
- v9: 6.0% (flag_get) → 89.2% (distance) — massively underreported

## Files Modified

- `scripts/evaluate_all_models.py` — added `--render`, timestamps, best episode logging
- `~/.config/nvim/lua/plugins/database.lua` — removed hardcoded DB connection

## Code / Commands Used

```bash
# Eval with rendering for recording (planned)
poetry run python scripts/evaluate_all_models.py --render --num-episodes 5

# Headless eval for statistics (planned)
poetry run python scripts/evaluate_all_models.py --num-episodes 50
```

## Next Steps

- [ ] Run headless eval (50 episodes/model) for final stats — deferred to Mar 14
- [ ] Run `--render` eval for video recording
- [ ] Use LosslessCut to trim best episode per model from recording
- [x] v9 training complete — final model saved
- [ ] Full Jupyter notebook analysis (v9 vs all experiments)
- [ ] Phase 7: Final packaging and write-up

## Notes

The reward shaping fix is the single biggest improvement across all 9 experiments. v9 went from 0% (pre-fix models) to 89% completion with just the x_delta clamp and stage-based completion bonus. The agent was always capable — it just needed the right reward signal.

Also set up Codex CLI v0.104.0 via dedicated Nix profile (`~/.local/state/nix/profiles/codex`) — the correct pattern for third-party flakes on ZenaOS to avoid hijacking the zix-managed default profile.

## Related

- [[ProjectDocumentation]]
- [[Tasks Dashboard]]
